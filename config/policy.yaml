policies:
  - identity: local-dev
    allow:
      - service: openai
        actions:
          - responses.create
      - service: anthropic
        actions:
          - '*'
      - service: github
        actions:
          - '*'
